inputDir: 'src/test/resources/methodSplitting/'
outputDir: 'output'

parser:
  type: 'antlr'
  extensions: ['java', 'js']

filters:
  - type: 'by tree size'
    maxTreeSize: 1000

problem:
  type: 'file name'

storage:
  type: 'code2vec paths'
  maxPathLength: 5
  maxPathWidth: 5
